1. pin versions in requirements
2. split train.py to smaller modules 
3. windows support (https://github.com/Skovorp/feral/issues/11)
4. docstrings or type hints
5. test coverage for:
    - Dataset loading / JSON parsing
    - Model forward pass
    - Metric calculations (mAP, F1)
    - Chunk ensembling logic
    - Data augmentation pipeline
    - probably other components that I'm missing.
    - fix  tests.py:35 and tests.py:39 both define test_no_chunks. The
6. hardcoded paths? move old configs to some other place?
7. Add information how to run wandb server locally 
8. pip install feral: pyproject.toml with [project.scripts]
9. add release and versions to feral 
10. remove requirement for labels.json for inference. add class_names to checkpoint
11. no command line prompts
12. frame id encoding logic - simplify?. - metrics.py:91 parses frame IDs with x[0].split('_chunkind_')[1] — fragile if a filename ever contains that substring. You already use a proper regex at metrics.py:102; just reuse it. This is sub-task of your 12.
    13. run not at 256 by 256?
14. move the video processing script to repo?
15. improve handling of wrong input in run.py 
16. fix/remove distributed train?
17. Documentation:
    - config parameters
    - benchmarks on different datasets/hardware
18. offline logging, so you dont need wandb. set wandb=disabled in this case
19. Replace print with logging 
20. handle bfloat16
21. f1 threshold 0.85
22. remove commented out code
23. add code to validate config and labels.json 
24. what to do with torch.backends.cuda.enable_math_sdp(False)
25. add validation for loading checkpoints
    26. add support for vjepa5.1 and videoprism
